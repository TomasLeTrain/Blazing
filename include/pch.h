#pragma once

// precompiled header file. Should only include header files that won't change
// good for increasing compilation times for header only libraries

#include <concepts>
#include <typeinfo>
#include <optional>
#include <functional>
#include <array>
#include <vector>
#include <list>
#include <mutex>

#include "api.h"

#include "units/all.hpp"
